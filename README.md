# E-commerce Backend

This is the backend for the e-commerce application.

## Getting Started

### Development Server

To start the development server with automatic recompilation and restarts on file changes, use the following command:

```bash
npm run dev
```

This command utilizes `ts-node-dev` to watch for changes in your `src` directory, transpile TypeScript files, and restart the server automatically.
